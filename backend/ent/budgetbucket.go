// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"measix/platform/ent/budgetbucket"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// BudgetBucket is the model entity for the BudgetBucket schema.
type BudgetBucket struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ScopeKey holds the value of the "scope_key" field.
	ScopeKey string `json:"scope_key,omitempty"`
	// Period holds the value of the "period" field.
	Period budgetbucket.Period `json:"period,omitempty"`
	// PeriodStart holds the value of the "period_start" field.
	PeriodStart time.Time `json:"period_start,omitempty"`
	// PeriodEnd holds the value of the "period_end" field.
	PeriodEnd *time.Time `json:"period_end,omitempty"`
	// Meter holds the value of the "meter" field.
	Meter budgetbucket.Meter `json:"meter,omitempty"`
	// SettledQuantity holds the value of the "settled_quantity" field.
	SettledQuantity int64 `json:"settled_quantity,omitempty"`
	// ReservedQuantity holds the value of the "reserved_quantity" field.
	ReservedQuantity int64 `json:"reserved_quantity,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*BudgetBucket) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case budgetbucket.FieldID, budgetbucket.FieldSettledQuantity, budgetbucket.FieldReservedQuantity:
			values[i] = new(sql.NullInt64)
		case budgetbucket.FieldScopeKey, budgetbucket.FieldPeriod, budgetbucket.FieldMeter:
			values[i] = new(sql.NullString)
		case budgetbucket.FieldPeriodStart, budgetbucket.FieldPeriodEnd, budgetbucket.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the BudgetBucket fields.
func (_m *BudgetBucket) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case budgetbucket.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case budgetbucket.FieldScopeKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field scope_key", values[i])
			} else if value.Valid {
				_m.ScopeKey = value.String
			}
		case budgetbucket.FieldPeriod:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field period", values[i])
			} else if value.Valid {
				_m.Period = budgetbucket.Period(value.String)
			}
		case budgetbucket.FieldPeriodStart:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field period_start", values[i])
			} else if value.Valid {
				_m.PeriodStart = value.Time
			}
		case budgetbucket.FieldPeriodEnd:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field period_end", values[i])
			} else if value.Valid {
				_m.PeriodEnd = new(time.Time)
				*_m.PeriodEnd = value.Time
			}
		case budgetbucket.FieldMeter:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field meter", values[i])
			} else if value.Valid {
				_m.Meter = budgetbucket.Meter(value.String)
			}
		case budgetbucket.FieldSettledQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field settled_quantity", values[i])
			} else if value.Valid {
				_m.SettledQuantity = value.Int64
			}
		case budgetbucket.FieldReservedQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field reserved_quantity", values[i])
			} else if value.Valid {
				_m.ReservedQuantity = value.Int64
			}
		case budgetbucket.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the BudgetBucket.
// This includes values selected through modifiers, order, etc.
func (_m *BudgetBucket) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this BudgetBucket.
// Note that you need to call BudgetBucket.Unwrap() before calling this method if this BudgetBucket
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *BudgetBucket) Update() *BudgetBucketUpdateOne {
	return NewBudgetBucketClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the BudgetBucket entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *BudgetBucket) Unwrap() *BudgetBucket {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: BudgetBucket is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *BudgetBucket) String() string {
	var builder strings.Builder
	builder.WriteString("BudgetBucket(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("scope_key=")
	builder.WriteString(_m.ScopeKey)
	builder.WriteString(", ")
	builder.WriteString("period=")
	builder.WriteString(fmt.Sprintf("%v", _m.Period))
	builder.WriteString(", ")
	builder.WriteString("period_start=")
	builder.WriteString(_m.PeriodStart.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.PeriodEnd; v != nil {
		builder.WriteString("period_end=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("meter=")
	builder.WriteString(fmt.Sprintf("%v", _m.Meter))
	builder.WriteString(", ")
	builder.WriteString("settled_quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.SettledQuantity))
	builder.WriteString(", ")
	builder.WriteString("reserved_quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.ReservedQuantity))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// BudgetBuckets is a parsable slice of BudgetBucket.
type BudgetBuckets []*BudgetBucket
