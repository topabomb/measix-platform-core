name: Apply Upstream List Green

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize
    paths:
      - .github/workflows/apply-upstream-list.yml

permissions:
  contents: write

env:
  GO_VERSION: '1.26.5'
  NODE_VERSION: '24'
  PNPM_VERSION: '11.0.0'

jobs:
  apply-green:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
      - name: Apply verified source patch
        run: git apply .github/patches/apply-upstream-list.patch
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false
      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          package-manager-cache: false
      - name: Pin pnpm
        run: |
          corepack enable
          corepack prepare pnpm@${PNPM_VERSION} --activate
      - uses: ariga/setup-atlas@v0
        with:
          version: v1.2.0
      - name: Generate and verify focused Green
        run: |
          make generate
          cd backend
          go test ./internal/contract ./internal/hub/upstream -count=1
          cd ../console
          pnpm test --run
      - name: Commit verified product changes
        run: |
          git config user.name 'measix-ci'
          git config user.email 'actions@users.noreply.github.com'
          git add api backend console
          git diff --cached --check
          git commit -m 'feat: complete Admin upstream list contract'
          git push origin HEAD:refs/heads/${{ github.event.pull_request.head.ref }}
