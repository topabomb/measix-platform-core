// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/topabomb/measix-platform-core/backend/ent/upstream"
)

// Upstream is the model entity for the Upstream schema.
type Upstream struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// ConfigRevision holds the value of the "config_revision" field.
	ConfigRevision int64 `json:"config_revision,omitempty"`
	// ActiveConfigRevision holds the value of the "active_config_revision" field.
	ActiveConfigRevision *int64 `json:"active_config_revision,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Upstream) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case upstream.FieldConfigRevision, upstream.FieldActiveConfigRevision:
			values[i] = new(sql.NullInt64)
		case upstream.FieldID, upstream.FieldName, upstream.FieldStatus:
			values[i] = new(sql.NullString)
		case upstream.FieldCreatedAt, upstream.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Upstream fields.
func (_m *Upstream) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case upstream.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case upstream.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case upstream.FieldConfigRevision:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field config_revision", values[i])
			} else if value.Valid {
				_m.ConfigRevision = value.Int64
			}
		case upstream.FieldActiveConfigRevision:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field active_config_revision", values[i])
			} else if value.Valid {
				_m.ActiveConfigRevision = new(int64)
				*_m.ActiveConfigRevision = value.Int64
			}
		case upstream.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case upstream.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case upstream.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Upstream.
// This includes values selected through modifiers, order, etc.
func (_m *Upstream) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Upstream.
// Note that you need to call Upstream.Unwrap() before calling this method if this Upstream
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Upstream) Update() *UpstreamUpdateOne {
	return NewUpstreamClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Upstream entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Upstream) Unwrap() *Upstream {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Upstream is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Upstream) String() string {
	var builder strings.Builder
	builder.WriteString("Upstream(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("config_revision=")
	builder.WriteString(fmt.Sprintf("%v", _m.ConfigRevision))
	builder.WriteString(", ")
	if v := _m.ActiveConfigRevision; v != nil {
		builder.WriteString("active_config_revision=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Upstreams is a parsable slice of Upstream.
type Upstreams []*Upstream
