// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"measix/platform/ent/portalsession"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PortalSession is the model entity for the PortalSession schema.
type PortalSession struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// SessionID holds the value of the "session_id" field.
	SessionID string `json:"session_id,omitempty"`
	// Origin holds the value of the "origin" field.
	Origin string `json:"origin,omitempty"`
	// TicketDigest holds the value of the "ticket_digest" field.
	TicketDigest []byte `json:"ticket_digest,omitempty"`
	// CookieDigest holds the value of the "cookie_digest" field.
	CookieDigest *[]byte `json:"cookie_digest,omitempty"`
	// GrantExpiresAt holds the value of the "grant_expires_at" field.
	GrantExpiresAt time.Time `json:"grant_expires_at,omitempty"`
	// ExpiresAt holds the value of the "expires_at" field.
	ExpiresAt time.Time `json:"expires_at,omitempty"`
	// Consumed holds the value of the "consumed" field.
	Consumed bool `json:"consumed,omitempty"`
	// Revoked holds the value of the "revoked" field.
	Revoked      bool `json:"revoked,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PortalSession) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case portalsession.FieldTicketDigest, portalsession.FieldCookieDigest:
			values[i] = new([]byte)
		case portalsession.FieldConsumed, portalsession.FieldRevoked:
			values[i] = new(sql.NullBool)
		case portalsession.FieldID, portalsession.FieldSessionID, portalsession.FieldOrigin:
			values[i] = new(sql.NullString)
		case portalsession.FieldGrantExpiresAt, portalsession.FieldExpiresAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PortalSession fields.
func (_m *PortalSession) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case portalsession.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case portalsession.FieldSessionID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field session_id", values[i])
			} else if value.Valid {
				_m.SessionID = value.String
			}
		case portalsession.FieldOrigin:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field origin", values[i])
			} else if value.Valid {
				_m.Origin = value.String
			}
		case portalsession.FieldTicketDigest:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field ticket_digest", values[i])
			} else if value != nil {
				_m.TicketDigest = *value
			}
		case portalsession.FieldCookieDigest:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field cookie_digest", values[i])
			} else if value != nil {
				_m.CookieDigest = value
			}
		case portalsession.FieldGrantExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field grant_expires_at", values[i])
			} else if value.Valid {
				_m.GrantExpiresAt = value.Time
			}
		case portalsession.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = value.Time
			}
		case portalsession.FieldConsumed:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field consumed", values[i])
			} else if value.Valid {
				_m.Consumed = value.Bool
			}
		case portalsession.FieldRevoked:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field revoked", values[i])
			} else if value.Valid {
				_m.Revoked = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PortalSession.
// This includes values selected through modifiers, order, etc.
func (_m *PortalSession) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this PortalSession.
// Note that you need to call PortalSession.Unwrap() before calling this method if this PortalSession
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *PortalSession) Update() *PortalSessionUpdateOne {
	return NewPortalSessionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the PortalSession entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *PortalSession) Unwrap() *PortalSession {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: PortalSession is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *PortalSession) String() string {
	var builder strings.Builder
	builder.WriteString("PortalSession(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("session_id=")
	builder.WriteString(_m.SessionID)
	builder.WriteString(", ")
	builder.WriteString("origin=")
	builder.WriteString(_m.Origin)
	builder.WriteString(", ")
	builder.WriteString("ticket_digest=")
	builder.WriteString(fmt.Sprintf("%v", _m.TicketDigest))
	builder.WriteString(", ")
	if v := _m.CookieDigest; v != nil {
		builder.WriteString("cookie_digest=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("grant_expires_at=")
	builder.WriteString(_m.GrantExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("expires_at=")
	builder.WriteString(_m.ExpiresAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("consumed=")
	builder.WriteString(fmt.Sprintf("%v", _m.Consumed))
	builder.WriteString(", ")
	builder.WriteString("revoked=")
	builder.WriteString(fmt.Sprintf("%v", _m.Revoked))
	builder.WriteByte(')')
	return builder.String()
}

// PortalSessions is a parsable slice of PortalSession.
type PortalSessions []*PortalSession
