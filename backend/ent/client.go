// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"
	"reflect"

	"measix/platform/ent/migrate"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"measix/platform/ent/activation"
	"measix/platform/ent/deployment"
	"measix/platform/ent/device"
	"measix/platform/ent/enrollment"
	"measix/platform/ent/idempotencyrecord"
	"measix/platform/ent/manageddraft"
	"measix/platform/ent/managedrelease"
	"measix/platform/ent/managedstate"
	"measix/platform/ent/pricingrule"
	"measix/platform/ent/requestusage"
	"measix/platform/ent/secret"
	"measix/platform/ent/secretversion"
	"measix/platform/ent/semanticusage"
	"measix/platform/ent/session"
	"measix/platform/ent/upstream"
	"measix/platform/ent/upstreamconfigrevision"
	"measix/platform/ent/user"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// Activation is the client for interacting with the Activation builders.
	Activation *ActivationClient
	// Deployment is the client for interacting with the Deployment builders.
	Deployment *DeploymentClient
	// Device is the client for interacting with the Device builders.
	Device *DeviceClient
	// Enrollment is the client for interacting with the Enrollment builders.
	Enrollment *EnrollmentClient
	// IdempotencyRecord is the client for interacting with the IdempotencyRecord builders.
	IdempotencyRecord *IdempotencyRecordClient
	// ManagedDraft is the client for interacting with the ManagedDraft builders.
	ManagedDraft *ManagedDraftClient
	// ManagedRelease is the client for interacting with the ManagedRelease builders.
	ManagedRelease *ManagedReleaseClient
	// ManagedState is the client for interacting with the ManagedState builders.
	ManagedState *ManagedStateClient
	// PricingRule is the client for interacting with the PricingRule builders.
	PricingRule *PricingRuleClient
	// RequestUsage is the client for interacting with the RequestUsage builders.
	RequestUsage *RequestUsageClient
	// Secret is the client for interacting with the Secret builders.
	Secret *SecretClient
	// SecretVersion is the client for interacting with the SecretVersion builders.
	SecretVersion *SecretVersionClient
	// SemanticUsage is the client for interacting with the SemanticUsage builders.
	SemanticUsage *SemanticUsageClient
	// Session is the client for interacting with the Session builders.
	Session *SessionClient
	// Upstream is the client for interacting with the Upstream builders.
	Upstream *UpstreamClient
	// UpstreamConfigRevision is the client for interacting with the UpstreamConfigRevision builders.
	UpstreamConfigRevision *UpstreamConfigRevisionClient
	// User is the client for interacting with the User builders.
	User *UserClient
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	client := &Client{config: newConfig(opts...)}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.Activation = NewActivationClient(c.config)
	c.Deployment = NewDeploymentClient(c.config)
	c.Device = NewDeviceClient(c.config)
	c.Enrollment = NewEnrollmentClient(c.config)
	c.IdempotencyRecord = NewIdempotencyRecordClient(c.config)
	c.ManagedDraft = NewManagedDraftClient(c.config)
	c.ManagedRelease = NewManagedReleaseClient(c.config)
	c.ManagedState = NewManagedStateClient(c.config)
	c.PricingRule = NewPricingRuleClient(c.config)
	c.RequestUsage = NewRequestUsageClient(c.config)
	c.Secret = NewSecretClient(c.config)
	c.SecretVersion = NewSecretVersionClient(c.config)
	c.SemanticUsage = NewSemanticUsageClient(c.config)
	c.Session = NewSessionClient(c.config)
	c.Upstream = NewUpstreamClient(c.config)
	c.UpstreamConfigRevision = NewUpstreamConfigRevisionClient(c.config)
	c.User = NewUserClient(c.config)
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// newConfig creates a new config for the client.
func newConfig(opts ...Option) config {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	return cfg
}

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// ErrTxStarted is returned when trying to start a new transaction from a transactional client.
var ErrTxStarted = errors.New("ent: cannot start a transaction within a transaction")

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, ErrTxStarted
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:                    ctx,
		config:                 cfg,
		Activation:             NewActivationClient(cfg),
		Deployment:             NewDeploymentClient(cfg),
		Device:                 NewDeviceClient(cfg),
		Enrollment:             NewEnrollmentClient(cfg),
		IdempotencyRecord:      NewIdempotencyRecordClient(cfg),
		ManagedDraft:           NewManagedDraftClient(cfg),
		ManagedRelease:         NewManagedReleaseClient(cfg),
		ManagedState:           NewManagedStateClient(cfg),
		PricingRule:            NewPricingRuleClient(cfg),
		RequestUsage:           NewRequestUsageClient(cfg),
		Secret:                 NewSecretClient(cfg),
		SecretVersion:          NewSecretVersionClient(cfg),
		SemanticUsage:          NewSemanticUsageClient(cfg),
		Session:                NewSessionClient(cfg),
		Upstream:               NewUpstreamClient(cfg),
		UpstreamConfigRevision: NewUpstreamConfigRevisionClient(cfg),
		User:                   NewUserClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:                    ctx,
		config:                 cfg,
		Activation:             NewActivationClient(cfg),
		Deployment:             NewDeploymentClient(cfg),
		Device:                 NewDeviceClient(cfg),
		Enrollment:             NewEnrollmentClient(cfg),
		IdempotencyRecord:      NewIdempotencyRecordClient(cfg),
		ManagedDraft:           NewManagedDraftClient(cfg),
		ManagedRelease:         NewManagedReleaseClient(cfg),
		ManagedState:           NewManagedStateClient(cfg),
		PricingRule:            NewPricingRuleClient(cfg),
		RequestUsage:           NewRequestUsageClient(cfg),
		Secret:                 NewSecretClient(cfg),
		SecretVersion:          NewSecretVersionClient(cfg),
		SemanticUsage:          NewSemanticUsageClient(cfg),
		Session:                NewSessionClient(cfg),
		Upstream:               NewUpstreamClient(cfg),
		UpstreamConfigRevision: NewUpstreamConfigRevisionClient(cfg),
		User:                   NewUserClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		Activation.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	for _, n := range []interface{ Use(...Hook) }{
		c.Activation, c.Deployment, c.Device, c.Enrollment, c.IdempotencyRecord,
		c.ManagedDraft, c.ManagedRelease, c.ManagedState, c.PricingRule,
		c.RequestUsage, c.Secret, c.SecretVersion, c.SemanticUsage, c.Session,
		c.Upstream, c.UpstreamConfigRevision, c.User,
	} {
		n.Use(hooks...)
	}
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	for _, n := range []interface{ Intercept(...Interceptor) }{
		c.Activation, c.Deployment, c.Device, c.Enrollment, c.IdempotencyRecord,
		c.ManagedDraft, c.ManagedRelease, c.ManagedState, c.PricingRule,
		c.RequestUsage, c.Secret, c.SecretVersion, c.SemanticUsage, c.Session,
		c.Upstream, c.UpstreamConfigRevision, c.User,
	} {
		n.Intercept(interceptors...)
	}
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *ActivationMutation:
		return c.Activation.mutate(ctx, m)
	case *DeploymentMutation:
		return c.Deployment.mutate(ctx, m)
	case *DeviceMutation:
		return c.Device.mutate(ctx, m)
	case *EnrollmentMutation:
		return c.Enrollment.mutate(ctx, m)
	case *IdempotencyRecordMutation:
		return c.IdempotencyRecord.mutate(ctx, m)
	case *ManagedDraftMutation:
		return c.ManagedDraft.mutate(ctx, m)
	case *ManagedReleaseMutation:
		return c.ManagedRelease.mutate(ctx, m)
	case *ManagedStateMutation:
		return c.ManagedState.mutate(ctx, m)
	case *PricingRuleMutation:
		return c.PricingRule.mutate(ctx, m)
	case *RequestUsageMutation:
		return c.RequestUsage.mutate(ctx, m)
	case *SecretMutation:
		return c.Secret.mutate(ctx, m)
	case *SecretVersionMutation:
		return c.SecretVersion.mutate(ctx, m)
	case *SemanticUsageMutation:
		return c.SemanticUsage.mutate(ctx, m)
	case *SessionMutation:
		return c.Session.mutate(ctx, m)
	case *UpstreamMutation:
		return c.Upstream.mutate(ctx, m)
	case *UpstreamConfigRevisionMutation:
		return c.UpstreamConfigRevision.mutate(ctx, m)
	case *UserMutation:
		return c.User.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// ActivationClient is a client for the Activation schema.
type ActivationClient struct {
	config
}

// NewActivationClient returns a client for the Activation from the given config.
func NewActivationClient(c config) *ActivationClient {
	return &ActivationClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `activation.Hooks(f(g(h())))`.
func (c *ActivationClient) Use(hooks ...Hook) {
	c.hooks.Activation = append(c.hooks.Activation, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `activation.Intercept(f(g(h())))`.
func (c *ActivationClient) Intercept(interceptors ...Interceptor) {
	c.inters.Activation = append(c.inters.Activation, interceptors...)
}

// Create returns a builder for creating a Activation entity.
func (c *ActivationClient) Create() *ActivationCreate {
	mutation := newActivationMutation(c.config, OpCreate)
	return &ActivationCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Activation entities.
func (c *ActivationClient) CreateBulk(builders ...*ActivationCreate) *ActivationCreateBulk {
	return &ActivationCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *ActivationClient) MapCreateBulk(slice any, setFunc func(*ActivationCreate, int)) *ActivationCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &ActivationCreateBulk{err: fmt.Errorf("calling to ActivationClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*ActivationCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &ActivationCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Activation.
func (c *ActivationClient) Update() *ActivationUpdate {
	mutation := newActivationMutation(c.config, OpUpdate)
	return &ActivationUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ActivationClient) UpdateOne(_m *Activation) *ActivationUpdateOne {
	mutation := newActivationMutation(c.config, OpUpdateOne, withActivation(_m))
	return &ActivationUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ActivationClient) UpdateOneID(id string) *ActivationUpdateOne {
	mutation := newActivationMutation(c.config, OpUpdateOne, withActivationID(id))
	return &ActivationUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Activation.
func (c *ActivationClient) Delete() *ActivationDelete {
	mutation := newActivationMutation(c.config, OpDelete)
	return &ActivationDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *ActivationClient) DeleteOne(_m *Activation) *ActivationDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *ActivationClient) DeleteOneID(id string) *ActivationDeleteOne {
	builder := c.Delete().Where(activation.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ActivationDeleteOne{builder}
}

// Query returns a query builder for Activation.
func (c *ActivationClient) Query() *ActivationQuery {
	return &ActivationQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeActivation},
		inters: c.Interceptors(),
	}
}

// Get returns a Activation entity by its id.
func (c *ActivationClient) Get(ctx context.Context, id string) (*Activation, error) {
	return c.Query().Where(activation.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ActivationClient) GetX(ctx context.Context, id string) *Activation {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *ActivationClient) Hooks() []Hook {
	return c.hooks.Activation
}

// Interceptors returns the client interceptors.
func (c *ActivationClient) Interceptors() []Interceptor {
	return c.inters.Activation
}

func (c *ActivationClient) mutate(ctx context.Context, m *ActivationMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&ActivationCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&ActivationUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&ActivationUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&ActivationDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Activation mutation op: %q", m.Op())
	}
}

// DeploymentClient is a client for the Deployment schema.
type DeploymentClient struct {
	config
}

// NewDeploymentClient returns a client for the Deployment from the given config.
func NewDeploymentClient(c config) *DeploymentClient {
	return &DeploymentClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `deployment.Hooks(f(g(h())))`.
func (c *DeploymentClient) Use(hooks ...Hook) {
	c.hooks.Deployment = append(c.hooks.Deployment, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `deployment.Intercept(f(g(h())))`.
func (c *DeploymentClient) Intercept(interceptors ...Interceptor) {
	c.inters.Deployment = append(c.inters.Deployment, interceptors...)
}

// Create returns a builder for creating a Deployment entity.
func (c *DeploymentClient) Create() *DeploymentCreate {
	mutation := newDeploymentMutation(c.config, OpCreate)
	return &DeploymentCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Deployment entities.
func (c *DeploymentClient) CreateBulk(builders ...*DeploymentCreate) *DeploymentCreateBulk {
	return &DeploymentCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *DeploymentClient) MapCreateBulk(slice any, setFunc func(*DeploymentCreate, int)) *DeploymentCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &DeploymentCreateBulk{err: fmt.Errorf("calling to DeploymentClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*DeploymentCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &DeploymentCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Deployment.
func (c *DeploymentClient) Update() *DeploymentUpdate {
	mutation := newDeploymentMutation(c.config, OpUpdate)
	return &DeploymentUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *DeploymentClient) UpdateOne(_m *Deployment) *DeploymentUpdateOne {
	mutation := newDeploymentMutation(c.config, OpUpdateOne, withDeployment(_m))
	return &DeploymentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *DeploymentClient) UpdateOneID(id string) *DeploymentUpdateOne {
	mutation := newDeploymentMutation(c.config, OpUpdateOne, withDeploymentID(id))
	return &DeploymentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Deployment.
func (c *DeploymentClient) Delete() *DeploymentDelete {
	mutation := newDeploymentMutation(c.config, OpDelete)
	return &DeploymentDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *DeploymentClient) DeleteOne(_m *Deployment) *DeploymentDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *DeploymentClient) DeleteOneID(id string) *DeploymentDeleteOne {
	builder := c.Delete().Where(deployment.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &DeploymentDeleteOne{builder}
}

// Query returns a query builder for Deployment.
func (c *DeploymentClient) Query() *DeploymentQuery {
	return &DeploymentQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeDeployment},
		inters: c.Interceptors(),
	}
}

// Get returns a Deployment entity by its id.
func (c *DeploymentClient) Get(ctx context.Context, id string) (*Deployment, error) {
	return c.Query().Where(deployment.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *DeploymentClient) GetX(ctx context.Context, id string) *Deployment {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *DeploymentClient) Hooks() []Hook {
	return c.hooks.Deployment
}

// Interceptors returns the client interceptors.
func (c *DeploymentClient) Interceptors() []Interceptor {
	return c.inters.Deployment
}

func (c *DeploymentClient) mutate(ctx context.Context, m *DeploymentMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&DeploymentCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&DeploymentUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&DeploymentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&DeploymentDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Deployment mutation op: %q", m.Op())
	}
}

// DeviceClient is a client for the Device schema.
type DeviceClient struct {
	config
}

// NewDeviceClient returns a client for the Device from the given config.
func NewDeviceClient(c config) *DeviceClient {
	return &DeviceClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `device.Hooks(f(g(h())))`.
func (c *DeviceClient) Use(hooks ...Hook) {
	c.hooks.Device = append(c.hooks.Device, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `device.Intercept(f(g(h())))`.
func (c *DeviceClient) Intercept(interceptors ...Interceptor) {
	c.inters.Device = append(c.inters.Device, interceptors...)
}

// Create returns a builder for creating a Device entity.
func (c *DeviceClient) Create() *DeviceCreate {
	mutation := newDeviceMutation(c.config, OpCreate)
	return &DeviceCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Device entities.
func (c *DeviceClient) CreateBulk(builders ...*DeviceCreate) *DeviceCreateBulk {
	return &DeviceCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *DeviceClient) MapCreateBulk(slice any, setFunc func(*DeviceCreate, int)) *DeviceCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &DeviceCreateBulk{err: fmt.Errorf("calling to DeviceClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*DeviceCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &DeviceCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Device.
func (c *DeviceClient) Update() *DeviceUpdate {
	mutation := newDeviceMutation(c.config, OpUpdate)
	return &DeviceUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *DeviceClient) UpdateOne(_m *Device) *DeviceUpdateOne {
	mutation := newDeviceMutation(c.config, OpUpdateOne, withDevice(_m))
	return &DeviceUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *DeviceClient) UpdateOneID(id string) *DeviceUpdateOne {
	mutation := newDeviceMutation(c.config, OpUpdateOne, withDeviceID(id))
	return &DeviceUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Device.
func (c *DeviceClient) Delete() *DeviceDelete {
	mutation := newDeviceMutation(c.config, OpDelete)
	return &DeviceDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *DeviceClient) DeleteOne(_m *Device) *DeviceDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *DeviceClient) DeleteOneID(id string) *DeviceDeleteOne {
	builder := c.Delete().Where(device.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &DeviceDeleteOne{builder}
}

// Query returns a query builder for Device.
func (c *DeviceClient) Query() *DeviceQuery {
	return &DeviceQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeDevice},
		inters: c.Interceptors(),
	}
}

// Get returns a Device entity by its id.
func (c *DeviceClient) Get(ctx context.Context, id string) (*Device, error) {
	return c.Query().Where(device.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *DeviceClient) GetX(ctx context.Context, id string) *Device {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *DeviceClient) Hooks() []Hook {
	return c.hooks.Device
}

// Interceptors returns the client interceptors.
func (c *DeviceClient) Interceptors() []Interceptor {
	return c.inters.Device
}

func (c *DeviceClient) mutate(ctx context.Context, m *DeviceMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&DeviceCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&DeviceUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&DeviceUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&DeviceDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Device mutation op: %q", m.Op())
	}
}

// EnrollmentClient is a client for the Enrollment schema.
type EnrollmentClient struct {
	config
}

// NewEnrollmentClient returns a client for the Enrollment from the given config.
func NewEnrollmentClient(c config) *EnrollmentClient {
	return &EnrollmentClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `enrollment.Hooks(f(g(h())))`.
func (c *EnrollmentClient) Use(hooks ...Hook) {
	c.hooks.Enrollment = append(c.hooks.Enrollment, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `enrollment.Intercept(f(g(h())))`.
func (c *EnrollmentClient) Intercept(interceptors ...Interceptor) {
	c.inters.Enrollment = append(c.inters.Enrollment, interceptors...)
}

// Create returns a builder for creating a Enrollment entity.
func (c *EnrollmentClient) Create() *EnrollmentCreate {
	mutation := newEnrollmentMutation(c.config, OpCreate)
	return &EnrollmentCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Enrollment entities.
func (c *EnrollmentClient) CreateBulk(builders ...*EnrollmentCreate) *EnrollmentCreateBulk {
	return &EnrollmentCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *EnrollmentClient) MapCreateBulk(slice any, setFunc func(*EnrollmentCreate, int)) *EnrollmentCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &EnrollmentCreateBulk{err: fmt.Errorf("calling to EnrollmentClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*EnrollmentCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &EnrollmentCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Enrollment.
func (c *EnrollmentClient) Update() *EnrollmentUpdate {
	mutation := newEnrollmentMutation(c.config, OpUpdate)
	return &EnrollmentUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *EnrollmentClient) UpdateOne(_m *Enrollment) *EnrollmentUpdateOne {
	mutation := newEnrollmentMutation(c.config, OpUpdateOne, withEnrollment(_m))
	return &EnrollmentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *EnrollmentClient) UpdateOneID(id string) *EnrollmentUpdateOne {
	mutation := newEnrollmentMutation(c.config, OpUpdateOne, withEnrollmentID(id))
	return &EnrollmentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Enrollment.
func (c *EnrollmentClient) Delete() *EnrollmentDelete {
	mutation := newEnrollmentMutation(c.config, OpDelete)
	return &EnrollmentDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *EnrollmentClient) DeleteOne(_m *Enrollment) *EnrollmentDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *EnrollmentClient) DeleteOneID(id string) *EnrollmentDeleteOne {
	builder := c.Delete().Where(enrollment.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &EnrollmentDeleteOne{builder}
}

// Query returns a query builder for Enrollment.
func (c *EnrollmentClient) Query() *EnrollmentQuery {
	return &EnrollmentQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeEnrollment},
		inters: c.Interceptors(),
	}
}

// Get returns a Enrollment entity by its id.
func (c *EnrollmentClient) Get(ctx context.Context, id string) (*Enrollment, error) {
	return c.Query().Where(enrollment.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *EnrollmentClient) GetX(ctx context.Context, id string) *Enrollment {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *EnrollmentClient) Hooks() []Hook {
	return c.hooks.Enrollment
}

// Interceptors returns the client interceptors.
func (c *EnrollmentClient) Interceptors() []Interceptor {
	return c.inters.Enrollment
}

func (c *EnrollmentClient) mutate(ctx context.Context, m *EnrollmentMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&EnrollmentCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&EnrollmentUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&EnrollmentUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&EnrollmentDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Enrollment mutation op: %q", m.Op())
	}
}

// IdempotencyRecordClient is a client for the IdempotencyRecord schema.
type IdempotencyRecordClient struct {
	config
}

// NewIdempotencyRecordClient returns a client for the IdempotencyRecord from the given config.
func NewIdempotencyRecordClient(c config) *IdempotencyRecordClient {
	return &IdempotencyRecordClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `idempotencyrecord.Hooks(f(g(h())))`.
func (c *IdempotencyRecordClient) Use(hooks ...Hook) {
	c.hooks.IdempotencyRecord = append(c.hooks.IdempotencyRecord, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `idempotencyrecord.Intercept(f(g(h())))`.
func (c *IdempotencyRecordClient) Intercept(interceptors ...Interceptor) {
	c.inters.IdempotencyRecord = append(c.inters.IdempotencyRecord, interceptors...)
}

// Create returns a builder for creating a IdempotencyRecord entity.
func (c *IdempotencyRecordClient) Create() *IdempotencyRecordCreate {
	mutation := newIdempotencyRecordMutation(c.config, OpCreate)
	return &IdempotencyRecordCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of IdempotencyRecord entities.
func (c *IdempotencyRecordClient) CreateBulk(builders ...*IdempotencyRecordCreate) *IdempotencyRecordCreateBulk {
	return &IdempotencyRecordCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *IdempotencyRecordClient) MapCreateBulk(slice any, setFunc func(*IdempotencyRecordCreate, int)) *IdempotencyRecordCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &IdempotencyRecordCreateBulk{err: fmt.Errorf("calling to IdempotencyRecordClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*IdempotencyRecordCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &IdempotencyRecordCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for IdempotencyRecord.
func (c *IdempotencyRecordClient) Update() *IdempotencyRecordUpdate {
	mutation := newIdempotencyRecordMutation(c.config, OpUpdate)
	return &IdempotencyRecordUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *IdempotencyRecordClient) UpdateOne(_m *IdempotencyRecord) *IdempotencyRecordUpdateOne {
	mutation := newIdempotencyRecordMutation(c.config, OpUpdateOne, withIdempotencyRecord(_m))
	return &IdempotencyRecordUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *IdempotencyRecordClient) UpdateOneID(id int) *IdempotencyRecordUpdateOne {
	mutation := newIdempotencyRecordMutation(c.config, OpUpdateOne, withIdempotencyRecordID(id))
	return &IdempotencyRecordUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for IdempotencyRecord.
func (c *IdempotencyRecordClient) Delete() *IdempotencyRecordDelete {
	mutation := newIdempotencyRecordMutation(c.config, OpDelete)
	return &IdempotencyRecordDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *IdempotencyRecordClient) DeleteOne(_m *IdempotencyRecord) *IdempotencyRecordDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *IdempotencyRecordClient) DeleteOneID(id int) *IdempotencyRecordDeleteOne {
	builder := c.Delete().Where(idempotencyrecord.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &IdempotencyRecordDeleteOne{builder}
}

// Query returns a query builder for IdempotencyRecord.
func (c *IdempotencyRecordClient) Query() *IdempotencyRecordQuery {
	return &IdempotencyRecordQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeIdempotencyRecord},
		inters: c.Interceptors(),
	}
}

// Get returns a IdempotencyRecord entity by its id.
func (c *IdempotencyRecordClient) Get(ctx context.Context, id int) (*IdempotencyRecord, error) {
	return c.Query().Where(idempotencyrecord.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *IdempotencyRecordClient) GetX(ctx context.Context, id int) *IdempotencyRecord {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *IdempotencyRecordClient) Hooks() []Hook {
	return c.hooks.IdempotencyRecord
}

// Interceptors returns the client interceptors.
func (c *IdempotencyRecordClient) Interceptors() []Interceptor {
	return c.inters.IdempotencyRecord
}

func (c *IdempotencyRecordClient) mutate(ctx context.Context, m *IdempotencyRecordMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&IdempotencyRecordCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&IdempotencyRecordUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&IdempotencyRecordUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&IdempotencyRecordDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown IdempotencyRecord mutation op: %q", m.Op())
	}
}

// ManagedDraftClient is a client for the ManagedDraft schema.
type ManagedDraftClient struct {
	config
}

// NewManagedDraftClient returns a client for the ManagedDraft from the given config.
func NewManagedDraftClient(c config) *ManagedDraftClient {
	return &ManagedDraftClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `manageddraft.Hooks(f(g(h())))`.
func (c *ManagedDraftClient) Use(hooks ...Hook) {
	c.hooks.ManagedDraft = append(c.hooks.ManagedDraft, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `manageddraft.Intercept(f(g(h())))`.
func (c *ManagedDraftClient) Intercept(interceptors ...Interceptor) {
	c.inters.ManagedDraft = append(c.inters.ManagedDraft, interceptors...)
}

// Create returns a builder for creating a ManagedDraft entity.
func (c *ManagedDraftClient) Create() *ManagedDraftCreate {
	mutation := newManagedDraftMutation(c.config, OpCreate)
	return &ManagedDraftCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of ManagedDraft entities.
func (c *ManagedDraftClient) CreateBulk(builders ...*ManagedDraftCreate) *ManagedDraftCreateBulk {
	return &ManagedDraftCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *ManagedDraftClient) MapCreateBulk(slice any, setFunc func(*ManagedDraftCreate, int)) *ManagedDraftCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &ManagedDraftCreateBulk{err: fmt.Errorf("calling to ManagedDraftClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*ManagedDraftCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &ManagedDraftCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for ManagedDraft.
func (c *ManagedDraftClient) Update() *ManagedDraftUpdate {
	mutation := newManagedDraftMutation(c.config, OpUpdate)
	return &ManagedDraftUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ManagedDraftClient) UpdateOne(_m *ManagedDraft) *ManagedDraftUpdateOne {
	mutation := newManagedDraftMutation(c.config, OpUpdateOne, withManagedDraft(_m))
	return &ManagedDraftUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ManagedDraftClient) UpdateOneID(id string) *ManagedDraftUpdateOne {
	mutation := newManagedDraftMutation(c.config, OpUpdateOne, withManagedDraftID(id))
	return &ManagedDraftUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for ManagedDraft.
func (c *ManagedDraftClient) Delete() *ManagedDraftDelete {
	mutation := newManagedDraftMutation(c.config, OpDelete)
	return &ManagedDraftDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *ManagedDraftClient) DeleteOne(_m *ManagedDraft) *ManagedDraftDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *ManagedDraftClient) DeleteOneID(id string) *ManagedDraftDeleteOne {
	builder := c.Delete().Where(manageddraft.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ManagedDraftDeleteOne{builder}
}

// Query returns a query builder for ManagedDraft.
func (c *ManagedDraftClient) Query() *ManagedDraftQuery {
	return &ManagedDraftQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeManagedDraft},
		inters: c.Interceptors(),
	}
}

// Get returns a ManagedDraft entity by its id.
func (c *ManagedDraftClient) Get(ctx context.Context, id string) (*ManagedDraft, error) {
	return c.Query().Where(manageddraft.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ManagedDraftClient) GetX(ctx context.Context, id string) *ManagedDraft {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *ManagedDraftClient) Hooks() []Hook {
	return c.hooks.ManagedDraft
}

// Interceptors returns the client interceptors.
func (c *ManagedDraftClient) Interceptors() []Interceptor {
	return c.inters.ManagedDraft
}

func (c *ManagedDraftClient) mutate(ctx context.Context, m *ManagedDraftMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&ManagedDraftCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&ManagedDraftUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&ManagedDraftUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&ManagedDraftDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown ManagedDraft mutation op: %q", m.Op())
	}
}

// ManagedReleaseClient is a client for the ManagedRelease schema.
type ManagedReleaseClient struct {
	config
}

// NewManagedReleaseClient returns a client for the ManagedRelease from the given config.
func NewManagedReleaseClient(c config) *ManagedReleaseClient {
	return &ManagedReleaseClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `managedrelease.Hooks(f(g(h())))`.
func (c *ManagedReleaseClient) Use(hooks ...Hook) {
	c.hooks.ManagedRelease = append(c.hooks.ManagedRelease, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `managedrelease.Intercept(f(g(h())))`.
func (c *ManagedReleaseClient) Intercept(interceptors ...Interceptor) {
	c.inters.ManagedRelease = append(c.inters.ManagedRelease, interceptors...)
}

// Create returns a builder for creating a ManagedRelease entity.
func (c *ManagedReleaseClient) Create() *ManagedReleaseCreate {
	mutation := newManagedReleaseMutation(c.config, OpCreate)
	return &ManagedReleaseCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of ManagedRelease entities.
func (c *ManagedReleaseClient) CreateBulk(builders ...*ManagedReleaseCreate) *ManagedReleaseCreateBulk {
	return &ManagedReleaseCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *ManagedReleaseClient) MapCreateBulk(slice any, setFunc func(*ManagedReleaseCreate, int)) *ManagedReleaseCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &ManagedReleaseCreateBulk{err: fmt.Errorf("calling to ManagedReleaseClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*ManagedReleaseCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &ManagedReleaseCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for ManagedRelease.
func (c *ManagedReleaseClient) Update() *ManagedReleaseUpdate {
	mutation := newManagedReleaseMutation(c.config, OpUpdate)
	return &ManagedReleaseUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ManagedReleaseClient) UpdateOne(_m *ManagedRelease) *ManagedReleaseUpdateOne {
	mutation := newManagedReleaseMutation(c.config, OpUpdateOne, withManagedRelease(_m))
	return &ManagedReleaseUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ManagedReleaseClient) UpdateOneID(id string) *ManagedReleaseUpdateOne {
	mutation := newManagedReleaseMutation(c.config, OpUpdateOne, withManagedReleaseID(id))
	return &ManagedReleaseUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for ManagedRelease.
func (c *ManagedReleaseClient) Delete() *ManagedReleaseDelete {
	mutation := newManagedReleaseMutation(c.config, OpDelete)
	return &ManagedReleaseDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *ManagedReleaseClient) DeleteOne(_m *ManagedRelease) *ManagedReleaseDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *ManagedReleaseClient) DeleteOneID(id string) *ManagedReleaseDeleteOne {
	builder := c.Delete().Where(managedrelease.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ManagedReleaseDeleteOne{builder}
}

// Query returns a query builder for ManagedRelease.
func (c *ManagedReleaseClient) Query() *ManagedReleaseQuery {
	return &ManagedReleaseQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeManagedRelease},
		inters: c.Interceptors(),
	}
}

// Get returns a ManagedRelease entity by its id.
func (c *ManagedReleaseClient) Get(ctx context.Context, id string) (*ManagedRelease, error) {
	return c.Query().Where(managedrelease.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ManagedReleaseClient) GetX(ctx context.Context, id string) *ManagedRelease {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *ManagedReleaseClient) Hooks() []Hook {
	return c.hooks.ManagedRelease
}

// Interceptors returns the client interceptors.
func (c *ManagedReleaseClient) Interceptors() []Interceptor {
	return c.inters.ManagedRelease
}

func (c *ManagedReleaseClient) mutate(ctx context.Context, m *ManagedReleaseMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&ManagedReleaseCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&ManagedReleaseUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&ManagedReleaseUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&ManagedReleaseDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown ManagedRelease mutation op: %q", m.Op())
	}
}

// ManagedStateClient is a client for the ManagedState schema.
type ManagedStateClient struct {
	config
}

// NewManagedStateClient returns a client for the ManagedState from the given config.
func NewManagedStateClient(c config) *ManagedStateClient {
	return &ManagedStateClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `managedstate.Hooks(f(g(h())))`.
func (c *ManagedStateClient) Use(hooks ...Hook) {
	c.hooks.ManagedState = append(c.hooks.ManagedState, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `managedstate.Intercept(f(g(h())))`.
func (c *ManagedStateClient) Intercept(interceptors ...Interceptor) {
	c.inters.ManagedState = append(c.inters.ManagedState, interceptors...)
}

// Create returns a builder for creating a ManagedState entity.
func (c *ManagedStateClient) Create() *ManagedStateCreate {
	mutation := newManagedStateMutation(c.config, OpCreate)
	return &ManagedStateCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of ManagedState entities.
func (c *ManagedStateClient) CreateBulk(builders ...*ManagedStateCreate) *ManagedStateCreateBulk {
	return &ManagedStateCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *ManagedStateClient) MapCreateBulk(slice any, setFunc func(*ManagedStateCreate, int)) *ManagedStateCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &ManagedStateCreateBulk{err: fmt.Errorf("calling to ManagedStateClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*ManagedStateCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &ManagedStateCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for ManagedState.
func (c *ManagedStateClient) Update() *ManagedStateUpdate {
	mutation := newManagedStateMutation(c.config, OpUpdate)
	return &ManagedStateUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *ManagedStateClient) UpdateOne(_m *ManagedState) *ManagedStateUpdateOne {
	mutation := newManagedStateMutation(c.config, OpUpdateOne, withManagedState(_m))
	return &ManagedStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *ManagedStateClient) UpdateOneID(id string) *ManagedStateUpdateOne {
	mutation := newManagedStateMutation(c.config, OpUpdateOne, withManagedStateID(id))
	return &ManagedStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for ManagedState.
func (c *ManagedStateClient) Delete() *ManagedStateDelete {
	mutation := newManagedStateMutation(c.config, OpDelete)
	return &ManagedStateDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *ManagedStateClient) DeleteOne(_m *ManagedState) *ManagedStateDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *ManagedStateClient) DeleteOneID(id string) *ManagedStateDeleteOne {
	builder := c.Delete().Where(managedstate.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &ManagedStateDeleteOne{builder}
}

// Query returns a query builder for ManagedState.
func (c *ManagedStateClient) Query() *ManagedStateQuery {
	return &ManagedStateQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeManagedState},
		inters: c.Interceptors(),
	}
}

// Get returns a ManagedState entity by its id.
func (c *ManagedStateClient) Get(ctx context.Context, id string) (*ManagedState, error) {
	return c.Query().Where(managedstate.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *ManagedStateClient) GetX(ctx context.Context, id string) *ManagedState {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *ManagedStateClient) Hooks() []Hook {
	return c.hooks.ManagedState
}

// Interceptors returns the client interceptors.
func (c *ManagedStateClient) Interceptors() []Interceptor {
	return c.inters.ManagedState
}

func (c *ManagedStateClient) mutate(ctx context.Context, m *ManagedStateMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&ManagedStateCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&ManagedStateUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&ManagedStateUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&ManagedStateDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown ManagedState mutation op: %q", m.Op())
	}
}

// PricingRuleClient is a client for the PricingRule schema.
type PricingRuleClient struct {
	config
}

// NewPricingRuleClient returns a client for the PricingRule from the given config.
func NewPricingRuleClient(c config) *PricingRuleClient {
	return &PricingRuleClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `pricingrule.Hooks(f(g(h())))`.
func (c *PricingRuleClient) Use(hooks ...Hook) {
	c.hooks.PricingRule = append(c.hooks.PricingRule, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `pricingrule.Intercept(f(g(h())))`.
func (c *PricingRuleClient) Intercept(interceptors ...Interceptor) {
	c.inters.PricingRule = append(c.inters.PricingRule, interceptors...)
}

// Create returns a builder for creating a PricingRule entity.
func (c *PricingRuleClient) Create() *PricingRuleCreate {
	mutation := newPricingRuleMutation(c.config, OpCreate)
	return &PricingRuleCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of PricingRule entities.
func (c *PricingRuleClient) CreateBulk(builders ...*PricingRuleCreate) *PricingRuleCreateBulk {
	return &PricingRuleCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *PricingRuleClient) MapCreateBulk(slice any, setFunc func(*PricingRuleCreate, int)) *PricingRuleCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &PricingRuleCreateBulk{err: fmt.Errorf("calling to PricingRuleClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*PricingRuleCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &PricingRuleCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for PricingRule.
func (c *PricingRuleClient) Update() *PricingRuleUpdate {
	mutation := newPricingRuleMutation(c.config, OpUpdate)
	return &PricingRuleUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *PricingRuleClient) UpdateOne(_m *PricingRule) *PricingRuleUpdateOne {
	mutation := newPricingRuleMutation(c.config, OpUpdateOne, withPricingRule(_m))
	return &PricingRuleUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *PricingRuleClient) UpdateOneID(id string) *PricingRuleUpdateOne {
	mutation := newPricingRuleMutation(c.config, OpUpdateOne, withPricingRuleID(id))
	return &PricingRuleUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for PricingRule.
func (c *PricingRuleClient) Delete() *PricingRuleDelete {
	mutation := newPricingRuleMutation(c.config, OpDelete)
	return &PricingRuleDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *PricingRuleClient) DeleteOne(_m *PricingRule) *PricingRuleDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *PricingRuleClient) DeleteOneID(id string) *PricingRuleDeleteOne {
	builder := c.Delete().Where(pricingrule.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &PricingRuleDeleteOne{builder}
}

// Query returns a query builder for PricingRule.
func (c *PricingRuleClient) Query() *PricingRuleQuery {
	return &PricingRuleQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypePricingRule},
		inters: c.Interceptors(),
	}
}

// Get returns a PricingRule entity by its id.
func (c *PricingRuleClient) Get(ctx context.Context, id string) (*PricingRule, error) {
	return c.Query().Where(pricingrule.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *PricingRuleClient) GetX(ctx context.Context, id string) *PricingRule {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *PricingRuleClient) Hooks() []Hook {
	return c.hooks.PricingRule
}

// Interceptors returns the client interceptors.
func (c *PricingRuleClient) Interceptors() []Interceptor {
	return c.inters.PricingRule
}

func (c *PricingRuleClient) mutate(ctx context.Context, m *PricingRuleMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&PricingRuleCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&PricingRuleUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&PricingRuleUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&PricingRuleDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown PricingRule mutation op: %q", m.Op())
	}
}

// RequestUsageClient is a client for the RequestUsage schema.
type RequestUsageClient struct {
	config
}

// NewRequestUsageClient returns a client for the RequestUsage from the given config.
func NewRequestUsageClient(c config) *RequestUsageClient {
	return &RequestUsageClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `requestusage.Hooks(f(g(h())))`.
func (c *RequestUsageClient) Use(hooks ...Hook) {
	c.hooks.RequestUsage = append(c.hooks.RequestUsage, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `requestusage.Intercept(f(g(h())))`.
func (c *RequestUsageClient) Intercept(interceptors ...Interceptor) {
	c.inters.RequestUsage = append(c.inters.RequestUsage, interceptors...)
}

// Create returns a builder for creating a RequestUsage entity.
func (c *RequestUsageClient) Create() *RequestUsageCreate {
	mutation := newRequestUsageMutation(c.config, OpCreate)
	return &RequestUsageCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of RequestUsage entities.
func (c *RequestUsageClient) CreateBulk(builders ...*RequestUsageCreate) *RequestUsageCreateBulk {
	return &RequestUsageCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *RequestUsageClient) MapCreateBulk(slice any, setFunc func(*RequestUsageCreate, int)) *RequestUsageCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &RequestUsageCreateBulk{err: fmt.Errorf("calling to RequestUsageClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*RequestUsageCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &RequestUsageCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for RequestUsage.
func (c *RequestUsageClient) Update() *RequestUsageUpdate {
	mutation := newRequestUsageMutation(c.config, OpUpdate)
	return &RequestUsageUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *RequestUsageClient) UpdateOne(_m *RequestUsage) *RequestUsageUpdateOne {
	mutation := newRequestUsageMutation(c.config, OpUpdateOne, withRequestUsage(_m))
	return &RequestUsageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *RequestUsageClient) UpdateOneID(id int) *RequestUsageUpdateOne {
	mutation := newRequestUsageMutation(c.config, OpUpdateOne, withRequestUsageID(id))
	return &RequestUsageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for RequestUsage.
func (c *RequestUsageClient) Delete() *RequestUsageDelete {
	mutation := newRequestUsageMutation(c.config, OpDelete)
	return &RequestUsageDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *RequestUsageClient) DeleteOne(_m *RequestUsage) *RequestUsageDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *RequestUsageClient) DeleteOneID(id int) *RequestUsageDeleteOne {
	builder := c.Delete().Where(requestusage.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &RequestUsageDeleteOne{builder}
}

// Query returns a query builder for RequestUsage.
func (c *RequestUsageClient) Query() *RequestUsageQuery {
	return &RequestUsageQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeRequestUsage},
		inters: c.Interceptors(),
	}
}

// Get returns a RequestUsage entity by its id.
func (c *RequestUsageClient) Get(ctx context.Context, id int) (*RequestUsage, error) {
	return c.Query().Where(requestusage.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *RequestUsageClient) GetX(ctx context.Context, id int) *RequestUsage {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *RequestUsageClient) Hooks() []Hook {
	return c.hooks.RequestUsage
}

// Interceptors returns the client interceptors.
func (c *RequestUsageClient) Interceptors() []Interceptor {
	return c.inters.RequestUsage
}

func (c *RequestUsageClient) mutate(ctx context.Context, m *RequestUsageMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&RequestUsageCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&RequestUsageUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&RequestUsageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&RequestUsageDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown RequestUsage mutation op: %q", m.Op())
	}
}

// SecretClient is a client for the Secret schema.
type SecretClient struct {
	config
}

// NewSecretClient returns a client for the Secret from the given config.
func NewSecretClient(c config) *SecretClient {
	return &SecretClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `secret.Hooks(f(g(h())))`.
func (c *SecretClient) Use(hooks ...Hook) {
	c.hooks.Secret = append(c.hooks.Secret, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `secret.Intercept(f(g(h())))`.
func (c *SecretClient) Intercept(interceptors ...Interceptor) {
	c.inters.Secret = append(c.inters.Secret, interceptors...)
}

// Create returns a builder for creating a Secret entity.
func (c *SecretClient) Create() *SecretCreate {
	mutation := newSecretMutation(c.config, OpCreate)
	return &SecretCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Secret entities.
func (c *SecretClient) CreateBulk(builders ...*SecretCreate) *SecretCreateBulk {
	return &SecretCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SecretClient) MapCreateBulk(slice any, setFunc func(*SecretCreate, int)) *SecretCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SecretCreateBulk{err: fmt.Errorf("calling to SecretClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SecretCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SecretCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Secret.
func (c *SecretClient) Update() *SecretUpdate {
	mutation := newSecretMutation(c.config, OpUpdate)
	return &SecretUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SecretClient) UpdateOne(_m *Secret) *SecretUpdateOne {
	mutation := newSecretMutation(c.config, OpUpdateOne, withSecret(_m))
	return &SecretUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SecretClient) UpdateOneID(id string) *SecretUpdateOne {
	mutation := newSecretMutation(c.config, OpUpdateOne, withSecretID(id))
	return &SecretUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Secret.
func (c *SecretClient) Delete() *SecretDelete {
	mutation := newSecretMutation(c.config, OpDelete)
	return &SecretDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SecretClient) DeleteOne(_m *Secret) *SecretDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SecretClient) DeleteOneID(id string) *SecretDeleteOne {
	builder := c.Delete().Where(secret.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SecretDeleteOne{builder}
}

// Query returns a query builder for Secret.
func (c *SecretClient) Query() *SecretQuery {
	return &SecretQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSecret},
		inters: c.Interceptors(),
	}
}

// Get returns a Secret entity by its id.
func (c *SecretClient) Get(ctx context.Context, id string) (*Secret, error) {
	return c.Query().Where(secret.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SecretClient) GetX(ctx context.Context, id string) *Secret {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *SecretClient) Hooks() []Hook {
	return c.hooks.Secret
}

// Interceptors returns the client interceptors.
func (c *SecretClient) Interceptors() []Interceptor {
	return c.inters.Secret
}

func (c *SecretClient) mutate(ctx context.Context, m *SecretMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SecretCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SecretUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SecretUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SecretDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Secret mutation op: %q", m.Op())
	}
}

// SecretVersionClient is a client for the SecretVersion schema.
type SecretVersionClient struct {
	config
}

// NewSecretVersionClient returns a client for the SecretVersion from the given config.
func NewSecretVersionClient(c config) *SecretVersionClient {
	return &SecretVersionClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `secretversion.Hooks(f(g(h())))`.
func (c *SecretVersionClient) Use(hooks ...Hook) {
	c.hooks.SecretVersion = append(c.hooks.SecretVersion, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `secretversion.Intercept(f(g(h())))`.
func (c *SecretVersionClient) Intercept(interceptors ...Interceptor) {
	c.inters.SecretVersion = append(c.inters.SecretVersion, interceptors...)
}

// Create returns a builder for creating a SecretVersion entity.
func (c *SecretVersionClient) Create() *SecretVersionCreate {
	mutation := newSecretVersionMutation(c.config, OpCreate)
	return &SecretVersionCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SecretVersion entities.
func (c *SecretVersionClient) CreateBulk(builders ...*SecretVersionCreate) *SecretVersionCreateBulk {
	return &SecretVersionCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SecretVersionClient) MapCreateBulk(slice any, setFunc func(*SecretVersionCreate, int)) *SecretVersionCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SecretVersionCreateBulk{err: fmt.Errorf("calling to SecretVersionClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SecretVersionCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SecretVersionCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SecretVersion.
func (c *SecretVersionClient) Update() *SecretVersionUpdate {
	mutation := newSecretVersionMutation(c.config, OpUpdate)
	return &SecretVersionUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SecretVersionClient) UpdateOne(_m *SecretVersion) *SecretVersionUpdateOne {
	mutation := newSecretVersionMutation(c.config, OpUpdateOne, withSecretVersion(_m))
	return &SecretVersionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SecretVersionClient) UpdateOneID(id int) *SecretVersionUpdateOne {
	mutation := newSecretVersionMutation(c.config, OpUpdateOne, withSecretVersionID(id))
	return &SecretVersionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SecretVersion.
func (c *SecretVersionClient) Delete() *SecretVersionDelete {
	mutation := newSecretVersionMutation(c.config, OpDelete)
	return &SecretVersionDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SecretVersionClient) DeleteOne(_m *SecretVersion) *SecretVersionDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SecretVersionClient) DeleteOneID(id int) *SecretVersionDeleteOne {
	builder := c.Delete().Where(secretversion.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SecretVersionDeleteOne{builder}
}

// Query returns a query builder for SecretVersion.
func (c *SecretVersionClient) Query() *SecretVersionQuery {
	return &SecretVersionQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSecretVersion},
		inters: c.Interceptors(),
	}
}

// Get returns a SecretVersion entity by its id.
func (c *SecretVersionClient) Get(ctx context.Context, id int) (*SecretVersion, error) {
	return c.Query().Where(secretversion.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SecretVersionClient) GetX(ctx context.Context, id int) *SecretVersion {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *SecretVersionClient) Hooks() []Hook {
	return c.hooks.SecretVersion
}

// Interceptors returns the client interceptors.
func (c *SecretVersionClient) Interceptors() []Interceptor {
	return c.inters.SecretVersion
}

func (c *SecretVersionClient) mutate(ctx context.Context, m *SecretVersionMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SecretVersionCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SecretVersionUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SecretVersionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SecretVersionDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown SecretVersion mutation op: %q", m.Op())
	}
}

// SemanticUsageClient is a client for the SemanticUsage schema.
type SemanticUsageClient struct {
	config
}

// NewSemanticUsageClient returns a client for the SemanticUsage from the given config.
func NewSemanticUsageClient(c config) *SemanticUsageClient {
	return &SemanticUsageClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `semanticusage.Hooks(f(g(h())))`.
func (c *SemanticUsageClient) Use(hooks ...Hook) {
	c.hooks.SemanticUsage = append(c.hooks.SemanticUsage, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `semanticusage.Intercept(f(g(h())))`.
func (c *SemanticUsageClient) Intercept(interceptors ...Interceptor) {
	c.inters.SemanticUsage = append(c.inters.SemanticUsage, interceptors...)
}

// Create returns a builder for creating a SemanticUsage entity.
func (c *SemanticUsageClient) Create() *SemanticUsageCreate {
	mutation := newSemanticUsageMutation(c.config, OpCreate)
	return &SemanticUsageCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of SemanticUsage entities.
func (c *SemanticUsageClient) CreateBulk(builders ...*SemanticUsageCreate) *SemanticUsageCreateBulk {
	return &SemanticUsageCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SemanticUsageClient) MapCreateBulk(slice any, setFunc func(*SemanticUsageCreate, int)) *SemanticUsageCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SemanticUsageCreateBulk{err: fmt.Errorf("calling to SemanticUsageClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SemanticUsageCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SemanticUsageCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for SemanticUsage.
func (c *SemanticUsageClient) Update() *SemanticUsageUpdate {
	mutation := newSemanticUsageMutation(c.config, OpUpdate)
	return &SemanticUsageUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SemanticUsageClient) UpdateOne(_m *SemanticUsage) *SemanticUsageUpdateOne {
	mutation := newSemanticUsageMutation(c.config, OpUpdateOne, withSemanticUsage(_m))
	return &SemanticUsageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SemanticUsageClient) UpdateOneID(id string) *SemanticUsageUpdateOne {
	mutation := newSemanticUsageMutation(c.config, OpUpdateOne, withSemanticUsageID(id))
	return &SemanticUsageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for SemanticUsage.
func (c *SemanticUsageClient) Delete() *SemanticUsageDelete {
	mutation := newSemanticUsageMutation(c.config, OpDelete)
	return &SemanticUsageDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SemanticUsageClient) DeleteOne(_m *SemanticUsage) *SemanticUsageDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SemanticUsageClient) DeleteOneID(id string) *SemanticUsageDeleteOne {
	builder := c.Delete().Where(semanticusage.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SemanticUsageDeleteOne{builder}
}

// Query returns a query builder for SemanticUsage.
func (c *SemanticUsageClient) Query() *SemanticUsageQuery {
	return &SemanticUsageQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSemanticUsage},
		inters: c.Interceptors(),
	}
}

// Get returns a SemanticUsage entity by its id.
func (c *SemanticUsageClient) Get(ctx context.Context, id string) (*SemanticUsage, error) {
	return c.Query().Where(semanticusage.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SemanticUsageClient) GetX(ctx context.Context, id string) *SemanticUsage {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *SemanticUsageClient) Hooks() []Hook {
	return c.hooks.SemanticUsage
}

// Interceptors returns the client interceptors.
func (c *SemanticUsageClient) Interceptors() []Interceptor {
	return c.inters.SemanticUsage
}

func (c *SemanticUsageClient) mutate(ctx context.Context, m *SemanticUsageMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SemanticUsageCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SemanticUsageUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SemanticUsageUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SemanticUsageDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown SemanticUsage mutation op: %q", m.Op())
	}
}

// SessionClient is a client for the Session schema.
type SessionClient struct {
	config
}

// NewSessionClient returns a client for the Session from the given config.
func NewSessionClient(c config) *SessionClient {
	return &SessionClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `session.Hooks(f(g(h())))`.
func (c *SessionClient) Use(hooks ...Hook) {
	c.hooks.Session = append(c.hooks.Session, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `session.Intercept(f(g(h())))`.
func (c *SessionClient) Intercept(interceptors ...Interceptor) {
	c.inters.Session = append(c.inters.Session, interceptors...)
}

// Create returns a builder for creating a Session entity.
func (c *SessionClient) Create() *SessionCreate {
	mutation := newSessionMutation(c.config, OpCreate)
	return &SessionCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Session entities.
func (c *SessionClient) CreateBulk(builders ...*SessionCreate) *SessionCreateBulk {
	return &SessionCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *SessionClient) MapCreateBulk(slice any, setFunc func(*SessionCreate, int)) *SessionCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &SessionCreateBulk{err: fmt.Errorf("calling to SessionClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*SessionCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &SessionCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Session.
func (c *SessionClient) Update() *SessionUpdate {
	mutation := newSessionMutation(c.config, OpUpdate)
	return &SessionUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *SessionClient) UpdateOne(_m *Session) *SessionUpdateOne {
	mutation := newSessionMutation(c.config, OpUpdateOne, withSession(_m))
	return &SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *SessionClient) UpdateOneID(id string) *SessionUpdateOne {
	mutation := newSessionMutation(c.config, OpUpdateOne, withSessionID(id))
	return &SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Session.
func (c *SessionClient) Delete() *SessionDelete {
	mutation := newSessionMutation(c.config, OpDelete)
	return &SessionDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *SessionClient) DeleteOne(_m *Session) *SessionDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *SessionClient) DeleteOneID(id string) *SessionDeleteOne {
	builder := c.Delete().Where(session.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &SessionDeleteOne{builder}
}

// Query returns a query builder for Session.
func (c *SessionClient) Query() *SessionQuery {
	return &SessionQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeSession},
		inters: c.Interceptors(),
	}
}

// Get returns a Session entity by its id.
func (c *SessionClient) Get(ctx context.Context, id string) (*Session, error) {
	return c.Query().Where(session.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *SessionClient) GetX(ctx context.Context, id string) *Session {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *SessionClient) Hooks() []Hook {
	return c.hooks.Session
}

// Interceptors returns the client interceptors.
func (c *SessionClient) Interceptors() []Interceptor {
	return c.inters.Session
}

func (c *SessionClient) mutate(ctx context.Context, m *SessionMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&SessionCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&SessionUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&SessionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&SessionDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Session mutation op: %q", m.Op())
	}
}

// UpstreamClient is a client for the Upstream schema.
type UpstreamClient struct {
	config
}

// NewUpstreamClient returns a client for the Upstream from the given config.
func NewUpstreamClient(c config) *UpstreamClient {
	return &UpstreamClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `upstream.Hooks(f(g(h())))`.
func (c *UpstreamClient) Use(hooks ...Hook) {
	c.hooks.Upstream = append(c.hooks.Upstream, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `upstream.Intercept(f(g(h())))`.
func (c *UpstreamClient) Intercept(interceptors ...Interceptor) {
	c.inters.Upstream = append(c.inters.Upstream, interceptors...)
}

// Create returns a builder for creating a Upstream entity.
func (c *UpstreamClient) Create() *UpstreamCreate {
	mutation := newUpstreamMutation(c.config, OpCreate)
	return &UpstreamCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Upstream entities.
func (c *UpstreamClient) CreateBulk(builders ...*UpstreamCreate) *UpstreamCreateBulk {
	return &UpstreamCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *UpstreamClient) MapCreateBulk(slice any, setFunc func(*UpstreamCreate, int)) *UpstreamCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &UpstreamCreateBulk{err: fmt.Errorf("calling to UpstreamClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*UpstreamCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &UpstreamCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Upstream.
func (c *UpstreamClient) Update() *UpstreamUpdate {
	mutation := newUpstreamMutation(c.config, OpUpdate)
	return &UpstreamUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UpstreamClient) UpdateOne(_m *Upstream) *UpstreamUpdateOne {
	mutation := newUpstreamMutation(c.config, OpUpdateOne, withUpstream(_m))
	return &UpstreamUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UpstreamClient) UpdateOneID(id string) *UpstreamUpdateOne {
	mutation := newUpstreamMutation(c.config, OpUpdateOne, withUpstreamID(id))
	return &UpstreamUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Upstream.
func (c *UpstreamClient) Delete() *UpstreamDelete {
	mutation := newUpstreamMutation(c.config, OpDelete)
	return &UpstreamDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *UpstreamClient) DeleteOne(_m *Upstream) *UpstreamDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *UpstreamClient) DeleteOneID(id string) *UpstreamDeleteOne {
	builder := c.Delete().Where(upstream.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UpstreamDeleteOne{builder}
}

// Query returns a query builder for Upstream.
func (c *UpstreamClient) Query() *UpstreamQuery {
	return &UpstreamQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeUpstream},
		inters: c.Interceptors(),
	}
}

// Get returns a Upstream entity by its id.
func (c *UpstreamClient) Get(ctx context.Context, id string) (*Upstream, error) {
	return c.Query().Where(upstream.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UpstreamClient) GetX(ctx context.Context, id string) *Upstream {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *UpstreamClient) Hooks() []Hook {
	return c.hooks.Upstream
}

// Interceptors returns the client interceptors.
func (c *UpstreamClient) Interceptors() []Interceptor {
	return c.inters.Upstream
}

func (c *UpstreamClient) mutate(ctx context.Context, m *UpstreamMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&UpstreamCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&UpstreamUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&UpstreamUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&UpstreamDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Upstream mutation op: %q", m.Op())
	}
}

// UpstreamConfigRevisionClient is a client for the UpstreamConfigRevision schema.
type UpstreamConfigRevisionClient struct {
	config
}

// NewUpstreamConfigRevisionClient returns a client for the UpstreamConfigRevision from the given config.
func NewUpstreamConfigRevisionClient(c config) *UpstreamConfigRevisionClient {
	return &UpstreamConfigRevisionClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `upstreamconfigrevision.Hooks(f(g(h())))`.
func (c *UpstreamConfigRevisionClient) Use(hooks ...Hook) {
	c.hooks.UpstreamConfigRevision = append(c.hooks.UpstreamConfigRevision, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `upstreamconfigrevision.Intercept(f(g(h())))`.
func (c *UpstreamConfigRevisionClient) Intercept(interceptors ...Interceptor) {
	c.inters.UpstreamConfigRevision = append(c.inters.UpstreamConfigRevision, interceptors...)
}

// Create returns a builder for creating a UpstreamConfigRevision entity.
func (c *UpstreamConfigRevisionClient) Create() *UpstreamConfigRevisionCreate {
	mutation := newUpstreamConfigRevisionMutation(c.config, OpCreate)
	return &UpstreamConfigRevisionCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of UpstreamConfigRevision entities.
func (c *UpstreamConfigRevisionClient) CreateBulk(builders ...*UpstreamConfigRevisionCreate) *UpstreamConfigRevisionCreateBulk {
	return &UpstreamConfigRevisionCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *UpstreamConfigRevisionClient) MapCreateBulk(slice any, setFunc func(*UpstreamConfigRevisionCreate, int)) *UpstreamConfigRevisionCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &UpstreamConfigRevisionCreateBulk{err: fmt.Errorf("calling to UpstreamConfigRevisionClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*UpstreamConfigRevisionCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &UpstreamConfigRevisionCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for UpstreamConfigRevision.
func (c *UpstreamConfigRevisionClient) Update() *UpstreamConfigRevisionUpdate {
	mutation := newUpstreamConfigRevisionMutation(c.config, OpUpdate)
	return &UpstreamConfigRevisionUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UpstreamConfigRevisionClient) UpdateOne(_m *UpstreamConfigRevision) *UpstreamConfigRevisionUpdateOne {
	mutation := newUpstreamConfigRevisionMutation(c.config, OpUpdateOne, withUpstreamConfigRevision(_m))
	return &UpstreamConfigRevisionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UpstreamConfigRevisionClient) UpdateOneID(id int) *UpstreamConfigRevisionUpdateOne {
	mutation := newUpstreamConfigRevisionMutation(c.config, OpUpdateOne, withUpstreamConfigRevisionID(id))
	return &UpstreamConfigRevisionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for UpstreamConfigRevision.
func (c *UpstreamConfigRevisionClient) Delete() *UpstreamConfigRevisionDelete {
	mutation := newUpstreamConfigRevisionMutation(c.config, OpDelete)
	return &UpstreamConfigRevisionDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *UpstreamConfigRevisionClient) DeleteOne(_m *UpstreamConfigRevision) *UpstreamConfigRevisionDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *UpstreamConfigRevisionClient) DeleteOneID(id int) *UpstreamConfigRevisionDeleteOne {
	builder := c.Delete().Where(upstreamconfigrevision.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UpstreamConfigRevisionDeleteOne{builder}
}

// Query returns a query builder for UpstreamConfigRevision.
func (c *UpstreamConfigRevisionClient) Query() *UpstreamConfigRevisionQuery {
	return &UpstreamConfigRevisionQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeUpstreamConfigRevision},
		inters: c.Interceptors(),
	}
}

// Get returns a UpstreamConfigRevision entity by its id.
func (c *UpstreamConfigRevisionClient) Get(ctx context.Context, id int) (*UpstreamConfigRevision, error) {
	return c.Query().Where(upstreamconfigrevision.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UpstreamConfigRevisionClient) GetX(ctx context.Context, id int) *UpstreamConfigRevision {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *UpstreamConfigRevisionClient) Hooks() []Hook {
	return c.hooks.UpstreamConfigRevision
}

// Interceptors returns the client interceptors.
func (c *UpstreamConfigRevisionClient) Interceptors() []Interceptor {
	return c.inters.UpstreamConfigRevision
}

func (c *UpstreamConfigRevisionClient) mutate(ctx context.Context, m *UpstreamConfigRevisionMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&UpstreamConfigRevisionCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&UpstreamConfigRevisionUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&UpstreamConfigRevisionUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&UpstreamConfigRevisionDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown UpstreamConfigRevision mutation op: %q", m.Op())
	}
}

// UserClient is a client for the User schema.
type UserClient struct {
	config
}

// NewUserClient returns a client for the User from the given config.
func NewUserClient(c config) *UserClient {
	return &UserClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `user.Hooks(f(g(h())))`.
func (c *UserClient) Use(hooks ...Hook) {
	c.hooks.User = append(c.hooks.User, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `user.Intercept(f(g(h())))`.
func (c *UserClient) Intercept(interceptors ...Interceptor) {
	c.inters.User = append(c.inters.User, interceptors...)
}

// Create returns a builder for creating a User entity.
func (c *UserClient) Create() *UserCreate {
	mutation := newUserMutation(c.config, OpCreate)
	return &UserCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of User entities.
func (c *UserClient) CreateBulk(builders ...*UserCreate) *UserCreateBulk {
	return &UserCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *UserClient) MapCreateBulk(slice any, setFunc func(*UserCreate, int)) *UserCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &UserCreateBulk{err: fmt.Errorf("calling to UserClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*UserCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &UserCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for User.
func (c *UserClient) Update() *UserUpdate {
	mutation := newUserMutation(c.config, OpUpdate)
	return &UserUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *UserClient) UpdateOne(_m *User) *UserUpdateOne {
	mutation := newUserMutation(c.config, OpUpdateOne, withUser(_m))
	return &UserUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *UserClient) UpdateOneID(id string) *UserUpdateOne {
	mutation := newUserMutation(c.config, OpUpdateOne, withUserID(id))
	return &UserUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for User.
func (c *UserClient) Delete() *UserDelete {
	mutation := newUserMutation(c.config, OpDelete)
	return &UserDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *UserClient) DeleteOne(_m *User) *UserDeleteOne {
	return c.DeleteOneID(_m.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *UserClient) DeleteOneID(id string) *UserDeleteOne {
	builder := c.Delete().Where(user.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &UserDeleteOne{builder}
}

// Query returns a query builder for User.
func (c *UserClient) Query() *UserQuery {
	return &UserQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeUser},
		inters: c.Interceptors(),
	}
}

// Get returns a User entity by its id.
func (c *UserClient) Get(ctx context.Context, id string) (*User, error) {
	return c.Query().Where(user.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *UserClient) GetX(ctx context.Context, id string) *User {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *UserClient) Hooks() []Hook {
	return c.hooks.User
}

// Interceptors returns the client interceptors.
func (c *UserClient) Interceptors() []Interceptor {
	return c.inters.User
}

func (c *UserClient) mutate(ctx context.Context, m *UserMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&UserCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&UserUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&UserUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&UserDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown User mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		Activation, Deployment, Device, Enrollment, IdempotencyRecord, ManagedDraft,
		ManagedRelease, ManagedState, PricingRule, RequestUsage, Secret, SecretVersion,
		SemanticUsage, Session, Upstream, UpstreamConfigRevision, User []ent.Hook
	}
	inters struct {
		Activation, Deployment, Device, Enrollment, IdempotencyRecord, ManagedDraft,
		ManagedRelease, ManagedState, PricingRule, RequestUsage, Secret, SecretVersion,
		SemanticUsage, Session, Upstream, UpstreamConfigRevision,
		User []ent.Interceptor
	}
)
